export const FPS = 30;

export const SIM_SIZE = 100;

// TODO: We want to get rid of this and pick ust one, once we have a feel for what feels best, simulation wise.
export const ITERATION_METHOD: "TopDown" | "BottomUp" | "Random" = "BottomUp";
